import { PositionFilterPipe } from './position-filter.pipe';

describe('PositionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
